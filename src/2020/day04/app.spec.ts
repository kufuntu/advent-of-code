import { splitInput } from './app';

const testData = ``;

const splitTestData = [
];

describe('Main entry', () => {
  it('returns the split input for the testData', () => {
    expect(splitInput(testData)).toEqual(splitTestData);
  });
});
